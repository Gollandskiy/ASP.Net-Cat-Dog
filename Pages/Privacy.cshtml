@page
@model PrivacyModel;
@{
    ViewData["Title"] = "Animals";
}

<h2>Animal Information</h2>

    @if (Model.AnimalViewModel.Dog.Name != null)
    {
        <h3>Dog</h3>
        <p>Name: @Model.AnimalViewModel.Dog.Name</p>
        <p>Sound: @Model.AnimalViewModel.Dog.Sound</p>
        <button onclick="makeSound('dog')">Make Sound</button>
    }
    else
    {
        <p>Dog information not available</p>
    }

      @if (Model.AnimalViewModel.Cat.Name != null)
{
    <div>
        <h3>Cat</h3>
        <p>Name: @Model.AnimalViewModel.Cat.Name</p>
        <p>Sound: @Model.AnimalViewModel.Cat.Sound</p>
        <button onclick="makeSound('cat')">Make Sound</button>
    </div>
}
else
{
    <p>Cat information not available</p>
}

<script>
    function makeSound(animal) {
        console.log('Making sound for ' + animal);
        $.ajax({
            type: "POST",
            url: "/Animal/MakeSound",
            data: { animal: animal },
            success: function (response) {
                alert(response);
            }
        });
    }
</script>
